export const metadata = {
  title: "Rank Monitor",
  description: "Project3 rank monitor"
};

export default function RootLayout({ children }) {
  return (
    <html lang="ko">
      <body>{children}</body>
    </html>
  );
}
